# Backlog

## Next

- replace regex extraction with parser-backed symbol analysis for TypeScript and JSX
- resolve relative imports to local files and symbols instead of external placeholders
- add dependency adapters for npm registry or user-supplied APIs
- support Vue Router and richer route config conventions in addition to Next.js and React Router
- emit MCP-friendly query endpoints or JSON indices

## Later

- compare current dependency versions against live registry data
- attach codemod suggestions to advisories
- generate richer domain grouping beyond top-level folders
- add VS Code extension and desktop client shells
